# Copy into a repo as .github/workflows/label-sync.yml
name: Sync labels

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch: {}

jobs:
  sync:
    uses: avalon-initiative/.github/.github/workflows/label-sync.yml@main
